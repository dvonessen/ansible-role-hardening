# {{ ansible_managed }}

# Disabling unused or vulnerable filesystem modules
{% for filesystem in hardening_mount_filesystems_blacklist | difference(hardening_mount_filesystems_whitelist) %}
install {{ filesystem }} /bin/true
{% endfor %}
